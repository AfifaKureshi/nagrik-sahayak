[build]
builder = "NIXPACKS"

[deploy]
# Use ${PORT} so Railway tells the app which port to use
startCommand = "cd backend && python -m uvicorn main:app --host 0.0.0.0 --port ${PORT:-8000} --proxy-headers --forwarded-allow-ips='*'"

[[services]]
internal_port = 8000
protocol = "http"
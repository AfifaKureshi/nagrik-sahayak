[build]
builder = "NIXPACKS"

[deploy]
startCommand = "cd backend && python -m uvicorn main:app --host 0.0.0.0 --port 8080 --proxy-headers --forwarded-allow-ips='*'"

[[services]]
internal_port = 8000
protocol = "http"
